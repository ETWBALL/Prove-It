# Prove-It
Proofs Project Side Quest

## Local setup

Run these once after pulling changes:

```bash
pnpm install
pnpm -w --filter @prove-it/db run generate
```

Then start the workspace:

```bash
npm run dev
```
